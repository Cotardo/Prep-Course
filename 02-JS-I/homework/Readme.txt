* Variables: Es como un recipiente en donde vamos a guardar algo que se va a utilizar mas tarde.

 * Strings: Es la forma de decirle a a al acomputadora que esos datos son solo texto y no representan ningun otro valor.
 
 * Funciones (argumentos, `return`): Es la forma en la que vamos a darle nombre a los objetos a los cuales les vamos a indicar cual sera su función.
 
 * Declaraciones `if`: Mediante if podremos asignar ciertas condiciones para que se si cumplen ejecuten las operaciones que designemos.
 
 * Valores booleanos (`true`, `false`):  Son los valores verdadero o falso que esperaran indicaciones para ejecutar operaciones si se cumple cualquiera de las dos o las dos: